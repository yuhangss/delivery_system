package com.example.delivery.common

import androidx.compose.runtime.mutableStateOf

object UIVariables {
    var avatarText = mutableStateOf("等待唤醒...")
    var inputText = mutableStateOf("")

    var showDialog = false
}